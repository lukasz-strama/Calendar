#ifndef CALENDAR_H
#define CALENDAR_H

// Function to display the calendar for a specific month
void displayCalendar(int selected_month, int current_month, int current_day);

// Add an extern declaration for the month_names array
extern const char *month_names[];

#endif
